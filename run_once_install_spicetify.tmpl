{{ if (and (eq .chezmoi.os "linux") (eq .isPersonalMachine true) (not (lookPath "spicetify"))) -}}
#!/bin/bash

curl -fsSL https://raw.githubusercontent.com/spicetify/cli/main/install.sh | sh
{{- end }}
